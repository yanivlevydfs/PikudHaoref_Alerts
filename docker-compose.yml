version: '3.8'

services:
  web:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./alerts_history.db:/app/alerts_history.db
      - ./config.json:/app/config.json
    environment:
      - PYTHONUNBUFFERED=1
    restart: always
